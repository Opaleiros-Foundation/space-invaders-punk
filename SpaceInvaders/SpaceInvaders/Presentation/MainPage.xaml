<Page
    Background="{ThemeResource BackgroundBrush}"
    NavigationCacheMode="Required"
    x:Class="SpaceInvaders.Presentation.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:local="using:SpaceInvaders.Presentation"
    xmlns:uen="using:Uno.Extensions.Navigation.UI"
    xmlns:um="using:Uno.Material"
    xmlns:utu="using:Uno.Toolkit.UI"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer IsTabStop="True">
        <Grid utu:SafeArea.Insets="VisibleBounds">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <utu:NavigationBar Content="{Binding Title}" />

            <StackPanel
                Grid.Row="1"
                HorizontalAlignment="Center"
                Spacing="16"
                VerticalAlignment="Center">

                <TextBox PlaceholderText="Enter your name:" Text="{Binding Name, Mode=TwoWay}" />

                <Button
                    AutomationProperties.AutomationId="ControllersPageButton"
                    Command="{Binding GoToControllers}"
                    Content="Go to Second Page" />
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
