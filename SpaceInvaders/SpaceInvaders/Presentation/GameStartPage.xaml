<Page
    x:Class="SpaceInvaders.Presentation.GameStartPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SpaceInvaders.Presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="RootGrid">
        <Grid.Background>
            <ImageBrush ImageSource="ms-appx:///Assets/background/background.png" Stretch="UniformToFill"/>
        </Grid.Background>

        <Canvas>
            <Image x:Name="PlayerImage"
                   Source="{Binding Player.SpritePath}"
                   Width="100"
                   Height="100"
                   Canvas.Left="{Binding Player.X, Mode=TwoWay}"
                   Canvas.Top="{Binding Player.Y, Mode=TwoWay}"/>

            <Image Source="{Binding Alien.SpritePath}"
                   Width="80"
                   Height="80"
                   Canvas.Left="{Binding Alien.X, Mode=TwoWay}"
                   Canvas.Top="{Binding Alien.Y, Mode=TwoWay}"/>
        </Canvas>
    </Grid>
</Page>
